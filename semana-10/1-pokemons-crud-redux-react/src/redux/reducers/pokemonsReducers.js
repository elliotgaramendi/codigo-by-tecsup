const initialState = {
  loading: false
};

const pokemonsReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pokemonsReducers;